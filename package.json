{
  "name": "uniquechat-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && mkdir -p public && cp -r client/dist/* public/",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
